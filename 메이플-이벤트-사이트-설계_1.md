# 메이플 이벤트 요약 사이트 — 설계

> **이 문서는 설계만 끝난 상태입니다. 구현은 아직 시작하지 않았습니다.**
> 새 세션에서 이 파일을 올리고 "0단계부터 시작하자"로 이어가면 됩니다.
> 최종 갱신: 2026-08-21

메이플스토리 이벤트를 요약해서 올리는 사이트. 글은 운영자 한 명만 쓰고, 방문자는 읽기만 합니다.

---

## 0. 새 세션에서 먼저 읽을 것

### 진행 상태

- [x] 설계 (이 문서)
- [ ] 0단계 — 로컬 개발 환경
- [ ] 1단계 — 모델과 DB
- [ ] 2단계 — 공개 화면
- [ ] 3단계 — 작성 화면
- [ ] 4단계 — 서버 배포

### 작업자 상황

- Django 경험은 있으나 오래돼서 감을 잃은 상태. **용어와 개념을 짚어가며 진행할 것**
- 이전에는 단일 HTML 파일을 FTP로 올리는 방식만 써봄. 빌드·배포 파이프라인은 처음
- 한 번에 많이 나가지 말고 한 단계씩 확인하며 진행

### 이미 논의해서 제외한 것 (다시 제안하지 말 것)

| 제외 항목 | 이유 |
|---|---|
| 프론트 프레임워크 전반 (React, Vue, Svelte, Alpine 등) | 관리할 상태가 거의 없음. 복잡도만 늘어남 |
| Next.js | Node 서버가 추가로 필요해짐 |
| DRF / REST API | 프론트 프레임워크를 안 쓰므로 불필요 |
| RDS | 비용. 같은 서버에 MySQL 직접 설치 |
| Lightsail | EC2로 확정 |
| 넥슨 공지 크롤러 | 공지 구조가 바뀌면 깨짐 |
| AI 자동 요약 | 직접 작성해서 올리기로 함 |
| 캐시 (Redis 등) | 트래픽이 생기면 그때 |
| 커뮤니티 / 다른 사람 글쓰기 | 범위 밖 |
| 무중단 배포 | 점검 방식으로 충분 |

---

## 1. 기술 스택

### 백엔드

| 항목 | 선택 |
|---|---|
| 프레임워크 | Django |
| DB | MySQL 8.0, `utf8mb4` |
| WSGI | Gunicorn |

### 프론트

| 항목 | 선택 |
|---|---|
| HTML 생성 | Django 템플릿 |
| CSS | Tailwind + DaisyUI (CDN) |
| 인터랙션 | 바닐라 JS, 필요한 만큼만 |
| 마크다운 렌더 | marked.js (CDN) |
| 빌드 과정 | 없음 |

프론트 프레임워크는 쓰지 않습니다. 전부 CDN이라 `npm`도 `node_modules`도 없습니다.

---

## 2. 서버 (4단계에서)

| 항목 | 값 |
|---|---|
| 서비스 | AWS EC2 |
| 인스턴스 | t4g.small (2vCPU / 2GB RAM, ARM) |
| 리전 | 서울 (ap-northeast-2) |
| OS | Ubuntu LTS |
| 디스크 | EBS gp3 30GB |
| IP | Elastic IP 1개 |
| DB | 같은 서버에 MySQL 직접 설치 |

메모리 2GB가 하한선. 1GB로는 마이그레이션 중 메모리가 부족해집니다.

### 인프라 구성

| 구성 | 선택 |
|---|---|
| 웹서버 | nginx |
| 프로세스 관리 | systemd |
| HTTPS | Let's Encrypt + certbot |
| 정적 파일 | nginx가 직접 서빙 |
| 업로드 이미지 | nginx가 직접 서빙 (`/media/`) |
| 감시 | UptimeRobot 5분 핑 |

### 비용

t4g.small $15 + EBS $3 + Elastic IP $3.6 = **월 약 $22 (3만원)**. 데이터 전송 월 100GB까지 무료.

프리티어 크레딧 $200을 6개월간 사용 → 첫 6개월은 실질 무료. 이후 계정이 비활성화되므로 유료 플랜 전환 필요.

**가입일 기록 후 5개월째 알림 설정할 것.**

### AWS 세팅 첫날 체크리스트

- [ ] AWS Budgets 예산 알림 ($10 / $30, 이메일)
- [ ] 보안 그룹: SSH(22)는 본인 IP만, HTTP(80)/HTTPS(443)만 전체 개방
- [ ] 루트 계정 MFA, 평소에는 IAM 사용자로 작업
- [ ] Elastic IP를 인스턴스에 연결 (미연결 시 과금)

---

## 3. 화면 구성

### 공개

| 경로 | 화면 |
|---|---|
| `/` | 이벤트 목록. 진행중 기본, 예정·종료는 필터 |
| `/events/<slug>/` | 상세 |
| `/archive/` | 지난 이벤트 |

필터는 링크로 처리합니다 (`/?status=upcoming`). 검색 노출과 URL 공유가 됩니다.

### 로그인 필요

| 경로 | 화면 |
|---|---|
| `/login/` | 계정 하나. 회원가입 없음 |
| `/events/write/` | 작성 |
| `/events/<slug>/edit/` | 수정 |
| (위장 경로) | Django Admin. `/admin/` 아닌 경로로 |

회원가입 URL은 만들지 않습니다.

### 목록 카드

썸네일 / 카테고리 배지 / 종료 임박 배지 / 제목 / 한 줄 요약 / 기간

보상은 넣지 않습니다.

### 상세 페이지

상단 버튼 세 개:

1. **3줄 요약** (기본)
2. **상세히 보기** — `body`가 비면 버튼 숨김
3. **공식 홈페이지** — 새 탭 링크

**세 내용을 전부 HTML에 담아 보내고 CSS로 숨깁니다.** 클릭 시 서버에서 받아오는 방식은 쓰지 않습니다. 검색봇이 본문을 못 읽게 됩니다.

넥슨 페이지를 iframe으로 넣지 않습니다.

### 작성 화면

Django 폼. 입력칸 세로 나열 + 저장 버튼. 꾸미지 않음.

- 이미지 파일 직접 업로드
- 모바일에서 쓸 수 있어야 함
- 마크다운 미리보기 (marked.js)
- localStorage 임시저장
- 보상 입력은 Django formset

---

## 4. 데이터 모델

```
Event
  title            제목
  slug             URL 조각 (영문 직접 입력, 자동 생성 안 함)
  category         이벤트 / 업데이트 / 캐시샵 / 쿠폰
  start_at         시작
  end_at           종료
  thumbnail        ImageField
  summary_short    목록 카드 한 줄
  summary_three    3줄 요약 (텍스트, 줄바꿈 구분)
  body             상세 본문 (마크다운, 비워둘 수 있음)
  source_url       넥슨 원문 링크
  is_published     발행 여부
  created_at / updated_at

Reward  (Event 1:N)
  name       보상 이름
  quantity   수량
  condition  획득 조건
  order      정렬 순서
```

### 저장하지 않고 계산

- `status` (예정/진행중/종료) — `start_at`, `end_at`으로 계산
- `days_left` — 마찬가지

컬럼으로 두면 날짜가 지날 때마다 갱신해야 하고 크론이 필요해집니다.

### 주의

- 슬러그는 한글 제목에서 자동 생성하지 않습니다. `slugify`를 돌리면 빈 문자열이 나옵니다
- `summary_three`는 `JSONField`가 아닌 텍스트. 두 줄이나 네 줄이 될 수도 있습니다

---

## 5. 진행 순서

**0단계 — 로컬 개발 환경**
파이썬 설치 확인, 가상환경, Django 설치, MySQL 설치, 프로젝트 생성.
로컬도 MySQL로 맞춥니다. SQLite와 문자셋·대소문자·날짜 처리가 달라서 나중에 배포할 때 터집니다.

**1단계 — 모델과 DB**
`Event`, `Reward` 모델 작성, 마이그레이션, Django Admin 등록.
이벤트 두어 개를 Admin으로 넣어서 DB가 도는지 확인.

**2단계 — 공개 화면**
`base.html` → 목록 → 상세 순서로.
목록 페이지 하나를 CSS까지 끝내보고 나서 다음으로 넘어갑니다.

**3단계 — 작성 화면**
로그인, 폼, 이미지 업로드, formset.

**4단계 — 서버 배포**
AWS 세팅, nginx, Gunicorn, systemd, HTTPS, 도메인 연결.

로컬에서 형태가 나온 뒤에 서버를 올립니다. 프리티어 6개월을 아낄 수 있습니다.

---

## 6. 배포 (4단계 이후)

### 변경 종류별

| 무엇을 바꾸나 | 어떻게 |
|---|---|
| 이벤트 글 | 사이트에서 저장. 배포 없음 |
| CSS·JS·이미지 | 업로드만. nginx가 바로 서빙 |
| 파이썬 코드·템플릿·모델 | 점검 걸고 배포 |

템플릿 HTML도 재시작이 필요합니다 (`DEBUG=False`면 Django가 캐싱).

### 점검 배포 절차

```
1. nginx 점검 페이지 켜기 (본인 IP는 예외)
2. mysqldump로 DB 백업
3. 코드 업로드
4. python manage.py migrate
5. systemctl restart gunicorn
6. 주요 페이지 3개 직접 확인
7. 점검 해제
```

### 규칙

- 배포 전 Git 커밋 (되돌릴 지점)
- 배포 전 `python manage.py check --deploy`
- 마이그레이션은 더하기만. 컬럼 삭제는 두 번에 나눠서
- 점검 중에 디버깅하지 않는다. 막히면 이전 코드로 되돌리고 나와서 원인을 찾는다

### 백업

- DB: 주기적으로 `mysqldump`를 로컬로 내려받기. 콘텐츠가 DB에만 존재합니다
- 서버: EBS 스냅샷

---

## 7. 미정

- [ ] 도메인
- [ ] DNS를 어디에 둘지
- [ ] 카테고리 4개(이벤트/업데이트/캐시샵/쿠폰)로 충분한지
- [ ] 종료일 없는 상시 이벤트 처리 방법
- [ ] 로컬 PC의 파이썬·MySQL 설치 상태
- [ ] AWS 계정 신규 생성 여부

---

## 8. 보안

**첫 커밋 전에 `.gitignore`부터.** 한 번 커밋된 키는 파일을 지워도 히스토리에 남습니다.

```gitignore
.env
*.sqlite3
__pycache__/
*.pyc
/media/
/staticfiles/
*.sql
db_backup/
.venv/
*.pem
```

절대 커밋 금지: `SECRET_KEY`, DB 비밀번호, EC2 키 페어(`.pem`), DB 덤프.

`django-environ`으로 `.env`에서 읽고, `.env.example`에는 키 이름만 적어 커밋합니다.

`settings.py`는 `base.py` / `local.py` / `production.py`로 나눕니다. 한 파일에서 `if`로 처리하면 언젠가 `DEBUG=True`인 채로 배포하게 됩니다.
